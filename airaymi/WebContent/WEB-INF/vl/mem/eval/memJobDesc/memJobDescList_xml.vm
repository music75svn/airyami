#*************************************************************************
* CLASS 명      : memJobDescList_xml
* 작 업 자      : 유연주
* 작 업 일      : 2017년03월 13일 
* 기    능      : 업무수행평가항목 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      유연주    2017년03월 13일      최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"            : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"EMP_NO"          : "$!{CS_HTML.txt2html($list.EMP_NO)}",
		"EMP_NM"          : "$!{CS_HTML.txt2html($list.EMP_NM)}",
		"EMP_NM_TMP"      : "$!{CS_HTML.txt2html($list.EMP_NM)}",
		"DEPT_NM"         : "$!{CS_HTML.txt2html($list.DEPT_NM)}",
		"CAST_NM"         : "$!{CS_HTML.txt2html($list.CAST_NM)}",
		"POS_NM"          : "$!{CS_HTML.txt2html($list.POS_NM)}",
		"SEND_YN"         : "$!{CS_HTML.txt2html($list.SEND_YN)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
