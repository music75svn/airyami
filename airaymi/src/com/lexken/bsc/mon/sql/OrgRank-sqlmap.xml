<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="bsc.mon.orgRank">

	<!--
	==================================================================   
	  # 설명	: typeAlias 선언
	  #	기능	: SETTING
	  #	TABLE	: 
	==================================================================
	-->
	<typeAlias alias="hashMap" 			type="java.util.HashMap" />

	<!--
	==================================================================   
	  # 설명	: 관점코드 목록 조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_CODE
	==================================================================
	-->
	<select id="getPerspectiveList" parameterClass="hashMap" resultClass="hashMap">
		SELECT PERSPECTIVE_ID, PERSPECTIVE_NM, 'SCORE_'||PERSPECTIVE_ID AS COL_NM
		  FROM BSC_PERSPECTIVE
		 WHERE YEAR = #findYear#
		   AND DELETE_DT IS NULL
		 ORDER BY SORT_ORDER
	</select>	 

	<!--
	==================================================================   
	  # 설명	: 관점코드 조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_CODE
	==================================================================
	-->
	<select id="getPerspectiveFirstId" parameterClass="hashMap" resultClass="java.lang.String">
		SELECT COL_NM
		  FROM (
		        SELECT PERSPECTIVE_ID, PERSPECTIVE_NM, 'SCORE_'||PERSPECTIVE_ID AS COL_NM
		          FROM BSC_PERSPECTIVE
		         WHERE YEAR = #findYear#
		           AND DELETE_DT IS NULL
		         ORDER BY SORT_ORDER
		       )
		WHERE ROWNUM = 1
	</select>
	
	<!--
	==================================================================   
	  # 설명	: 조직순위 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: 
	==================================================================
	-->
	<select id="getList" parameterClass="hashMap" resultClass="hashMap" remapResults="true">
	    SELECT A.YEAR, A.MON, A.ANAL_CYCLE, A.SC_DEPT_ID, B.SC_DEPT_NM
	         , ROUND(A.SCORE, 6) AS SCORE
	         , A.STATUS, F_CODE_NM('021',  A.STATUS, A.YEAR) STATUS_NM
	         , ROUND(A.CONVERSION_SCORE, 6) AS CONVERSION_SCORE
		     , RANK() OVER(ORDER BY A.SCORE DESC NULLS LAST, B.SORT_ORDER) RANK ,
		      
		     <iterate property="perspectiveList" conjunction=",">
		     	 ROUND(C.SCORE_$perspectiveList[]$, 2) AS SCORE_$perspectiveList[]$
		     </iterate>
		     
		FROM BSC_DEPTSCORE A
		     INNER JOIN BSC_SC_DEPT B ON A.YEAR = B.YEAR AND A.SC_DEPT_ID = B.SC_DEPT_ID AND B.DELETE_DT IS NULL
		     LEFT OUTER JOIN (SELECT A.YEAR, A.MON, A.ANAL_CYCLE, A.SC_DEPT_ID, 
		                            
		                           	<iterate property="perspectiveList" conjunction=",">
								     	MAX(CASE WHEN A.PERSPECTIVE_ID = '$perspectiveList[]$' THEN A.SCORE END) SCORE_$perspectiveList[]$
								    </iterate>
								     
		                        FROM BSC_PERSSCORE A
		                        WHERE A.YEAR = #findYear#
		                          AND A.MON = #findMon#
		                          AND A.ANAL_CYCLE = #findAnalCycle#
		                        GROUP BY A.YEAR, A.MON, A.ANAL_CYCLE, A.SC_DEPT_ID
		                     ) C ON A.YEAR = C.YEAR AND A.MON = C.MON AND A.ANAL_CYCLE = C.ANAL_CYCLE AND A.SC_DEPT_ID = C.SC_DEPT_ID
		WHERE A.YEAR = #findYear#
		  AND A.MON = #findMon#
		  AND A.ANAL_CYCLE = #findAnalCycle#
		  
		<isNotEmpty prepend="AND" property="findScDeptGrpId">
			B.SC_DEPT_GRP_ID = #findScDeptGrpId#
		</isNotEmpty>
		
		<isNotEmpty prepend="AND" property="findLevelId">
			B.LEVEL_ID = #findLevelId#
		</isNotEmpty>
			    
		ORDER BY RANK() OVER(ORDER BY A.SCORE DESC NULLS LAST, B.SORT_ORDER)

	</select>
	
	<!--
	==================================================================   
	  # 설명	: 평가그룹 조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_CODE
	==================================================================
	-->
	<select id="getScDeptGrp" parameterClass="hashMap" resultClass="hashMap">
		SELECT A.CODE_ID
			 , A.CODE_NM
		 FROM BSC_CODE A
		WHERE A.CODE_GRP_ID  = '003'
		  AND A.YEAR = #findYear#
		  AND A.DELETE_DT IS NULL
		ORDER BY A.SORT_ORDER
	</select>	
	
	
</sqlMap>


