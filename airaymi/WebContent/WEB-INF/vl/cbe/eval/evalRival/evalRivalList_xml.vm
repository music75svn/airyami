#*************************************************************************
* CLASS 명      : evalRivalList_xml
* 작 업 자      : 박경태
* 작 업 일      : 2012년 12월 8일 
* 기    능      : 경쟁그룹 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      박경태     2012년 12월 8일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"                 : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"EVAL_DEGREE_ID"       : "$!{CS_HTML.txt2html($list.EVAL_DEGREE_ID)}", 
		"RIVAL_GRP_ID"         : "$!{CS_HTML.txt2html($list.RIVAL_GRP_ID)}", 
		"RIVAL_GRP_NM"         : "$!{CS_HTML.txt2html($list.RIVAL_GRP_NM)}", 
		"DEPT_NM"              : "$!{CS_HTML.txt2html($list.DEPT_NM)}", 
		"JIKGUB_NM"            : "$!{CS_HTML.txt2html($list.JIKGUB_NM)}", 
		"WORK_TERM"            : "$!{CS_HTML.txt2html($list.WORK_TERM)}", 
		"USER_CNT"             : "$!{CS_HTML.txt2html($list.USER_CNT)}", 
		"REFL_DT"              : "$!{CS_FORMAT.dateFormat($list.REFL_DT)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
