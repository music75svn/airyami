<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="bsc.popup.cycleAsset">

	<!--
	==================================================================
	  # 설명	: typeAlias 선언
	  #	기능	: SETTING
	  #	TABLE	:
	==================================================================
	-->
	<typeAlias alias="hashMap" 			type="java.util.HashMap" />


	<!--
	==================================================================
	  # 설명	: 시스템항목관리 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getCountList" remapResults="true" parameterClass="hashMap" resultClass="hashMap">
		<isEqual property="findItemCd" compareValue="S068D001">
		SELECT SUBSTR (A.ACNT_YM, 1, 4) YEAR,
		       SUBSTR (A.ACNT_YM, 5, 2) MON, 
		       B.DEPT_ID,
		       A.CUR_AMT_1,
		       A.ACCT_NM,
		       C.DEPT_NM
		  FROM V_ABA0199M_002 A,
		       (SELECT '1300' DEPT_ID FROM DUAL
		        ) B
	LEFT OUTER JOIN BSC_DEPTINFO C
			ON B.DEPT_ID = C.DEPT_ID
		 WHERE A.ACNT_YM = #findYear# || #findMon# AND A.ACCT_CD IN ('51000000', '55010000')
		 </isEqual>
		 <isEqual property="findItemCd" compareValue="S068D002">
		SELECT SUBSTR (A.ACNT_YM, 1, 4) YEAR,
		       SUBSTR (A.ACNT_YM, 5, 2) MON,
		       B.DEPT_ID,
		       A.CUR_AMT_2,
		       A.ACCT_NM,
		       C.DEPT_NM
		  FROM V_ABA0199M_001 A,
		       (SELECT '1300' DEPT_ID FROM DUAL
		        ) B
		        LEFT OUTER JOIN BSC_DEPTINFO C
		        ON B.DEPT_ID = C.DEPT_ID
		 WHERE A.ACNT_YM = #findYear# || #findMon# AND A.ACCT_CD = '10000000'		 
		 </isEqual>
		  <isEqual property="findItemCd" compareValue="S068D003">
		SELECT SUBSTR (A.ACNT_YM, 1, 4) YEAR,
		       SUBSTR (A.ACNT_YM, 5, 2) MON,
		       B.DEPT_ID,
		       C.DEPT_NM,
		       A.ACCT_NM,
		       A.CUR_AMT_2
		  FROM V_ABA0199M_001 A,
		          (SELECT '1300' DEPT_ID FROM DUAL) B
		       LEFT OUTER JOIN
		          BSC_DEPTINFO C
		       ON B.DEPT_ID = C.DEPT_ID
		 WHERE A.ACNT_YM = #findYear# || #findMon# AND A.ACCT_CD = '12020801'
		</isEqual>
		<isEqual property="findItemCd" compareValue="S068D004">
		SELECT #findYear# AS YEAR,
		       #findMon# AS MON, 
		       B.DEPT_ID,
		       A.CUR_AMT_2,
		       A.ACCT_NM,
		       C.DEPT_NM
		  FROM V_ABA0199M_001 A,
		       (SELECT '1300' DEPT_ID FROM DUAL
		        ) B
		        LEFT OUTER JOIN BSC_DEPTINFO C
		                ON B.DEPT_ID = C.DEPT_ID
		 WHERE A.ACNT_YM = TO_CHAR (TO_NUMBER (#findYear# - 1)) || '12'
		       AND A.ACCT_CD = '10000000'
		</isEqual>		                     
		<isEqual property="findItemCd" compareValue="S068D005">
		SELECT #findYear# AS YEAR,
		       #findMon# AS MON,
		       B.DEPT_ID,
		       A.CUR_AMT_2,
		       A.ACCT_NM,
		       C.DEPT_NM
		  FROM V_ABA0199M_001 A,
		       (SELECT '1300' DEPT_ID FROM DUAL
		        ) B
		        LEFT OUTER JOIN BSC_DEPTINFO C
		                ON B.DEPT_ID = C.DEPT_ID
		 WHERE A.ACNT_YM = TO_CHAR (TO_NUMBER (#findYear# - 1)) || '12'
		       AND A.ACCT_CD = '12020801'
		</isEqual>		                     
	</select>
	
	<!--
	==================================================================
	  # 설명	: 시스템 연계항목 정보를 가져옴
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getItem" remapResults="true" parameterClass="hashMap" resultClass="hashMap">
          SELECT A.ITEM_CD, B.CAL_TYPE_COL_NM
            FROM BSC_SYSTEM_ITEM A
               , BSC_CAL_TYPE_COL B
           WHERE A.YEAR = #findYear#
             AND A.YEAR = B.YEAR
             AND A.ITEM_CD = B.ITEM_CD
             AND B.METRIC_ID = #findMetricId#
			 AND B.INSERT_GUBUN = '01'
           ORDER BY B.CAL_TYPE_COL_NM
	</select>
	
	<!--
	==================================================================
	  # 설명	: 부서조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_DEPTINFO
	==================================================================
	-->
	<select id="getDeptList" parameterClass="hashMap" resultClass="hashMap">
		
	SELECT A.DEPT_ID, B.DEPT_NM
		  FROM BSC_SC_DEPT_MAPPING A
	LEFT OUTER JOIN BSC_DEPTINFO B
			ON A.DEPT_ID = B.DEPT_ID
		 WHERE (YEAR, SC_DEPT_ID) IN
		          (SELECT YEAR, SC_DEPT_ID
		             FROM BSC_METRIC
		            WHERE (METRIC_GRP_ID, YEAR) IN
		                     (SELECT METRIC_GRP_ID, YEAR
		                        FROM BSC_METRIC
		                       WHERE YEAR = #findYear# AND METRIC_ID = #findMetricId#)
				AND DELETE_DT IS NULL)
  
	</select>
	
	<!--
	==================================================================
	  # 설명	: 시스템 연계 부서 목록 보기
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getScDeptMapping" parameterClass="hashMap" resultClass="java.lang.String">
		SELECT DEPT_ID
		  FROM ( SELECT DEPT_ID 
				   FROM BSC_SC_DEPT_MAPPING
				  WHERE YEAR = #findYear# 
				    AND SC_DEPT_ID = #findScDeptCd# ORDER BY DEPT_ID 
				    )
		 WHERE ROWNUM = 1
	</select>
	

</sqlMap>


