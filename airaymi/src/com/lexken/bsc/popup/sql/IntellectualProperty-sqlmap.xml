<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="bsc.popup.intellectualProperty">

	<!--
	==================================================================
	  # 설명	: typeAlias 선언
	  #	기능	: SETTING
	  #	TABLE	:
	==================================================================
	-->
	<typeAlias alias="hashMap" 			type="java.util.HashMap" />


	<!--
	==================================================================
	  # 설명	: 지적 재산권 실적 점수 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getList" parameterClass="hashMap" resultClass="hashMap">
		SELECT A.YEAR
            , A.EXEC_DEPT_CD
            , B.DEPT_NM
            , A. PAT_TC
            , A.PAT_TC_NM
            , A.PROP_RGHT_NM
            , A.REG_DATE
            , A.SUBJ_NM
            , A.APRV_TC_NM
            , A.INVR_NM_EMPN_NM
            , CASE WHEN PAT_TC = 'B06' THEN '1'
                        WHEN PAT_TC = 'B05' THEN '1'
                        WHEN PAT_TC = 'B03' THEN '4'
                        WHEN PAT_TC = 'B02' THEN '2'
                        WHEN PAT_TC = 'B01' THEN '3'
                        WHEN PAT_TC = 'A06' THEN '0.5'
                        WHEN PAT_TC = 'A05' THEN '0.5'
                        WHEN PAT_TC = 'A03' THEN '2'
                        WHEN PAT_TC = 'A02' THEN '0.5'
                        WHEN PAT_TC = 'A01' THEN '1'
            END SCORE
		FROM(
            SELECT SUBSTR (APLN_DATE, 1, 4) YEAR
                        , EXEC_DEPT_CD
                        , PAT_TC
                        , PAT_TC_NM
                        , PROP_RGHT_NM
                        , REG_DATE
                        , SUBJ_NM
                        , APRV_TC_NM
                        , INVR_NM_EMPN_NM
                        FROM V_GDA0157D_001
            UNION
            SELECT SUBSTR (REG_DATE, 1, 4) YEAR
                        , EXEC_DEPT_CD
                        , PAT_TC
                        , PAT_TC_NM
                        , PROP_RGHT_NM
                        , REG_DATE
                        , SUBJ_NM
                        , APRV_TC_NM
                        , INVR_NM_EMPN_NM
                        FROM V_GDA0157D_001) A
		LEFT OUTER JOIN BSC_DEPTINFO B
		ON A.EXEC_DEPT_CD = B.DEPT_ID
		WHERE A.YEAR = #findYear#
		AND EXEC_DEPT_CD IS NOT NULL
	</select>

	<!--
	==================================================================
	  # 설명	: 지적 재산권 실적 점수 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getList2" parameterClass="hashMap" resultClass="hashMap">
		SELECT SUBSTR (A.PPR_ANN_DATE, 1, 4) YEAR,
		       A.SUBJ_NO,
		       B.DEPT_NM,
		       A.SCIT_TC_NM,
		       A.SCMG_NM,
		       A.SCMG_AREA_NM,
		       A.MAIN_EMPN,
		       A.MAIN_EMPN_NM,
		       A.SUBJ_NM,
		       A.PPR_ANN_DATE,
		       A.SCI_YN,
		       A.SCIT_TC,
		       CASE
		          WHEN SCI_YN = '01' THEN '6'
		          WHEN SCI_YN = '02' THEN
		          CASE WHEN SCIT_TC = '01' THEN '4'
		               WHEN SCIT_TC = '02' THEN '2'
		               WHEN SCIT_TC = '03' THEN '2'
		               WHEN SCIT_TC = '04' THEN '1'
		          END
		       END
		          SCORE
		  FROM    V_GDA0156D_001 A
	LEFT OUTER JOIN BSC_DEPTINFO B
            ON A.SUBJ_NO = B.DEPT_ID
		 WHERE A.PPR_ANN_DATE IS NOT NULL AND SUBSTR (A.PPR_ANN_DATE, 1, 4) = #findYear#

	</select>

	<!--
	==================================================================
	  # 설명	: 시스템 연계 부서 목록 보기
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getScDeptMapping" parameterClass="hashMap" resultClass="java.lang.String">
		SELECT DEPT_ID
		  FROM ( SELECT DEPT_ID 
				   FROM BSC_SC_DEPT_MAPPING
				  WHERE YEAR = #findYear# 
				    AND SC_DEPT_ID = #findScDeptCd# ORDER BY DEPT_ID 
				    )
		 WHERE ROWNUM = 1
	</select>

	<!--
	==================================================================
	  # 설명	: 시스템 연계 부서 목록 보기
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getDeptInfo" parameterClass="hashMap" resultClass="hashMap">
		SELECT A.DEPT_ID, B.DEPT_NM
			  FROM BSC_SC_DEPT_MAPPING A
		LEFT OUTER JOIN BSC_DEPTINFO B
				ON A.DEPT_ID = B.DEPT_ID
			 WHERE (YEAR, SC_DEPT_ID) IN
			          (SELECT YEAR, SC_DEPT_ID
			             FROM BSC_METRIC
			            WHERE (METRIC_GRP_ID, YEAR) IN
			                     (SELECT METRIC_GRP_ID, YEAR
			                        FROM BSC_METRIC
			                       WHERE YEAR = #findYear# AND METRIC_ID = #findMetricId#)
					AND DELETE_DT IS NULL)
	
	</select>

	<!--
	==================================================================
	  # 설명	: 시스템 연계항목 정보를 가져옴
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getItem" remapResults="true" parameterClass="hashMap" resultClass="hashMap">
          SELECT A.ITEM_CD, B.CAL_TYPE_COL_NM
            FROM BSC_SYSTEM_ITEM A
               , BSC_CAL_TYPE_COL B
           WHERE A.YEAR = #findYear#
             AND A.YEAR = B.YEAR
             AND A.ITEM_CD = B.ITEM_CD
             AND B.METRIC_ID = #findMetricId#
             AND B.INSERT_GUBUN = '01'
           ORDER BY B.CAL_TYPE_COL_NM
	</select>

</sqlMap>


