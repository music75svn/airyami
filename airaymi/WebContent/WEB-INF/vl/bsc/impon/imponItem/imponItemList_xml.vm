#*************************************************************************
* CLASS 명      : imponItemList_xml
* 작 업 자      : 박경태
* 작 업 일      : 2012년 11월 30일 
* 기    능      : 평가항목 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      박경태     2012년 11월 30일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"                 : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"ITEM_GBN_NM"          : "$!{CS_HTML.txt2html($list.ITEM_GBN_NM)}", 
		"EVAL_DEGREE_ID"       : "$!{CS_HTML.txt2html($list.EVAL_DEGREE_ID)}", 
		"METRIC_GRP_ID"        : "$!{CS_HTML.txt2html($list.METRIC_GRP_ID)}", 
		"ITEM_ID"              : "$!{CS_HTML.txt2html($list.ITEM_ID)}", 
		"ITEM_NM"              : "$!{CS_HTML.txt2html($list.ITEM_NM)}", 
		#foreach($itemlist in $itemList)
		"DEPT_$!{CS_HTML.txt2html($itemlist.METRIC_ID)}" : "$!{CS_HTML.txt2html($list.get($itemlist.COL_NM))}",
		#end
		"ACT_RPT_INSERT_USER_NM" : "$!{CS_HTML.txt2html($list.ACT_RPT_INSERT_USER_NM)}",
		"SUM_WEIGHT" 		   : "$!{CS_HTML.txt2html($list.SUM_WEIGHT)}"
		
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
