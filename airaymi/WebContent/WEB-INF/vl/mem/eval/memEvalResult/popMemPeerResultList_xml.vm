#*************************************************************************
* CLASS 명      : popMemPeerResultList_xml
* 작 업 자      : 유연주
* 작 업 일      : 2017년04월 07일 
* 기    능      : 업무수행평가결과 동료평가목록 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      유연주    2017년04월 07일     최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"              : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"EVAL_DEPT_NM"      : "$!{CS_HTML.txt2html($list.EVAL_DEPT_NM)}",
		"EVAL_CAST_NM"      : "$!{CS_HTML.txt2html($list.EVAL_CAST_NM)}",
		"EVAL_POS_NM"       : "$!{CS_HTML.txt2html($list.EVAL_POS_NM)}",
		"EVAL_EMP_NO"       : "$!{CS_HTML.txt2html($list.EVAL_EMP_NO)}",
		"EVAL_EMP_NM"       : "$!{CS_HTML.txt2html($list.EVAL_EMP_NM)}",
		"CHANGE_SCORE"      : "$!{CS_HTML.txt2html($list.CHANGE_SCORE)}",
		"AVERAGE"           : "$!{CS_HTML.txt2html($list.AVERAGE)}",
		"STD_DEVIATION"     : "$!{CS_HTML.txt2html($list.STD_DEVIATION)}",
		"ADJUST_SCORE"      : "$!{CS_HTML.txt2html($list.ADJUST_SCORE)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
