<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="bsc.popup.useFacilitiesInspec">

	<!--
	==================================================================
	  # 설명	: typeAlias 선언
	  #	기능	: SETTING
	  #	TABLE	:
	==================================================================
	-->
	<typeAlias alias="hashMap" 			type="java.util.HashMap" />


	<!--
	==================================================================
	  # 설명	: 시스템항목관리 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getList" parameterClass="hashMap" resultClass="hashMap">
		<isEqual property="findDeptId" compareValue="2600">
			SELECT   A.INSP_YY
			       , A.INSP_MON
			       , A.INSP_KIND_TC
			       , A.INSP_KIND_TC_NM
			       , A.PROC_DEPT_CD
			       , B.DEPT_NM
			       , A.INSP_NM
			       , A.CON_CNT
			       , A.INCON_CNT
			       , A.ETC_CNT
			       , A.TOTAL_CNT
			  FROM V_GBM0170M_001 A
			  LEFT OUTER JOIN V_DEPTINFO B
			  ON A.PROC_DEPT_CD = B.DEPT_ID
			 WHERE INSP_YY = #findYear#
			   AND INSP_MON = #findMon#
			   AND INSP_KIND_TC IN ('03','04','05')
			 ORDER BY INSP_MON
		</isEqual>
		<isNotEqual property="findDeptId" compareValue="2600">
			SELECT   A.INSP_YY
			       , A.INSP_MON
			       , A.INSP_KIND_TC
			       , A.INSP_KIND_TC_NM
			       , A.PROC_DEPT_CD
			       , B.DEPT_NM
			       , A.INSP_NM
			       , A.CON_CNT
			       , A.INCON_CNT
			       , A.ETC_CNT
			       , A.TOTAL_CNT
			  FROM V_GBM0170M_001 A
			  LEFT OUTER JOIN V_DEPTINFO B
			  ON A.PROC_DEPT_CD = B.DEPT_ID
			 WHERE INSP_YY = #findYear#
			   AND INSP_MON = #findMon#
			   AND INSP_KIND_TC IN ('03','04','05')
			   AND PROC_DEPT_CD = #findDeptId#
			 ORDER BY INSP_MON
		</isNotEqual>
	</select>

	<!--
	==================================================================
	  # 설명	: 추진지사 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: V_GBM0170M_001
	==================================================================
	-->
	<select id="getDeptList" parameterClass="hashMap" resultClass="hashMap">
			SELECT A.DEPT_ID, B.DEPT_NM
			  FROM BSC_SC_DEPT_MAPPING A
		LEFT OUTER JOIN BSC_DEPTINFO B
				ON A.DEPT_ID = B.DEPT_ID
			 WHERE (YEAR, SC_DEPT_ID) IN
			          (SELECT YEAR, SC_DEPT_ID
			             FROM BSC_METRIC
			            WHERE (METRIC_GRP_ID, YEAR) IN
			                     (SELECT METRIC_GRP_ID, YEAR
			                        FROM BSC_METRIC
			                       WHERE YEAR = #findYear# AND METRIC_ID = #findMetricId#)
					AND DELETE_DT IS NULL)
	<![CDATA[		   AND OULEVEL < 2 ]]>
	</select>
	
	<!--
	==================================================================
	  # 설명	: 시스템 연계항목 정보를 가져옴
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getItem" remapResults="true" parameterClass="hashMap" resultClass="hashMap">
          SELECT A.ITEM_CD, B.CAL_TYPE_COL_NM
             FROM BSC_SYSTEM_ITEM A
                , BSC_CAL_TYPE_COL B
            WHERE A.YEAR = #findYear#
              AND A.YEAR = B.YEAR
              AND A.ITEM_CD = B.ITEM_CD
              AND B.METRIC_ID = #findMetricId#
              AND B.INSERT_GUBUN = '01'
            ORDER BY B.CAL_TYPE_COL_NM
	</select>
	
	<!--
	==================================================================
	  # 설명	: 부서조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_DEPTINFO
	==================================================================
	-->
	<select id="getScDeptMapping" parameterClass="hashMap" resultClass="java.lang.String">
		SELECT DEPT_ID
		  FROM ( SELECT DEPT_ID 
				   FROM BSC_SC_DEPT_MAPPING
				  WHERE YEAR = #findYear# 
				    AND SC_DEPT_ID = #findScDeptCd# ORDER BY DEPT_ID 
				    )
		 WHERE ROWNUM = 1
	</select>

<!--
	==================================================================
	  # 설명	: 시스템항목관리 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getList2" parameterClass="hashMap" resultClass="hashMap">
		<isEqual property="findDeptId" compareValue="2600">
			SELECT   A.INSP_YY
			       , A.INSP_MON
			       , A.INSP_KIND_TC
			       , A.INSP_KIND_TC_NM
			       , A.PROC_DEPT_CD
			       , B.DEPT_NM
			       , A.INSP_NM
			       , A.CON_CNT
			       , A.INCON_CNT
			       , A.ETC_CNT
			       , A.TOTAL_CNT
			  FROM V_GBM0170M_001 A
			  LEFT OUTER JOIN V_DEPTINFO B
			  ON A.PROC_DEPT_CD = B.DEPT_ID
			 WHERE INSP_YY = #findYear#
			   AND INSP_MON = #findMon#
			   AND INSP_KIND_TC IN ('11')
			 ORDER BY INSP_MON
		</isEqual>
		<isNotEqual property="findDeptId" compareValue="2600">
			SELECT   A.INSP_YY
			       , A.INSP_MON
			       , A.INSP_KIND_TC
			       , A.INSP_KIND_TC_NM
			       , A.PROC_DEPT_CD
			       , B.DEPT_NM
			       , A.INSP_NM
			       , A.CON_CNT
			       , A.INCON_CNT
			       , A.ETC_CNT
			       , A.TOTAL_CNT
			  FROM V_GBM0170M_001 A
			  LEFT OUTER JOIN V_DEPTINFO B
			  ON A.PROC_DEPT_CD = B.DEPT_ID
			 WHERE INSP_YY = #findYear#
			   AND INSP_MON = #findMon#
			   AND INSP_KIND_TC IN ('11')
			   AND PROC_DEPT_CD = #findDeptId#
			 ORDER BY INSP_MON
		</isNotEqual>
	</select>
	
</sqlMap>


