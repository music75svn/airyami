#*************************************************************************
* CLASS 명      : evalWeightList_xml
* 작 업 자      : 박경태
* 작 업 일      : 2012년 12월 7일 
* 기    능      : 가중치그룹 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      박경태     2012년 12월 7일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"                 : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"EVAL_DEGREE_ID"       : "$!{CS_HTML.txt2html($list.EVAL_DEGREE_ID)}", 
		"WEIGHT_GRP_ID"        : "$!{CS_HTML.txt2html($list.WEIGHT_GRP_ID)}", 
		"WEIGHT_GRP_NM"        : "$!{CS_HTML.txt2html($list.WEIGHT_GRP_NM)}", 
		"USER_CNT"             : "$!{CS_HTML.txt2html($list.USER_CNT)}", 
		"FRS_DEPT_WEIGHT"      : "$!{CS_HTML.txt2html($list.FRS_DEPT_WEIGHT)}", 
		"SEC_DEPT_WEIGHT"      : "$!{CS_HTML.txt2html($list.SEC_DEPT_WEIGHT)}", 
		"TEAM_WEIGHT"          : "$!{CS_HTML.txt2html($list.TEAM_WEIGHT)}", 
		"PRS_WEIGHT"           : "$!{CS_HTML.txt2html($list.PRS_WEIGHT)}", 
		"SORT_ORDER"           : "$!{CS_HTML.txt2html($list.SORT_ORDER)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
