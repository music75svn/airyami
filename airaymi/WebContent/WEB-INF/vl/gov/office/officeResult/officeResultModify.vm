#*************************************************************************
* CLASS 명      : officeResultModify
* 작 업 자      : 정철수 
* 작 업 일      : 2012년 11월 5일 
* 기    능      : 공공기관결과    
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자    작     업     일        변 경 내 용                비고
* ----  --------  -----------------  -------------------------  --------
*  1    정철수      2012년 11월 5일             최 초 작 업 
**************************************************************************# 

<script type="text/javascript" language="javascript" >
//<![CDATA[

	var J = jQuery;

	J(document).ready(function(){
    
		/******************************************
		 * 취소버튼 (돌아가기)
		 ******************************************/
		J("#btnBack").click(function() {
			J("#form1").attr("action", "$!{context_path}/gov/office/officeResult/officeResultList.vw?type=template").submit();
		});
        
		/******************************************
		 * 저장버튼 이벤트
		 ******************************************/
		J("#btnInsert").click(function(e) {
			e.preventDefault();
			saveChk();
		});

		/******************************************
		 * 저장버튼 이중 클릭 방지
		 ******************************************/
		J("form").submit(function(){
			J("#btnInsert").unbind();
		});
		

		/******************************************
		 * 정렬순서 숫자만 입력
		 ******************************************/
		 
		
		$("#finalGrade").alpha({allow:"+-"}); //영문자만 입력 허용
		$("#finalGrade").css("ime-mode", "disabled"); //한글사용 비활성화
		
		J("#score").numeric({allow:"."});
		J("#score").css("ime-mode", "disabled");
		J("#rank").numeric();
		J("#rank").css("ime-mode", "disabled");
		J("#measScore").numeric({allow:"."});
		J("#measScore").css("ime-mode", "disabled");
		J("#measRank").numeric();
		J("#measRank").css("ime-mode", "disabled");
		J("#unmeasScore").numeric({allow:"."});
		J("#unmeasScore").css("ime-mode", "disabled");
		J("#unmeasRank").numeric();
		J("#unmeasRank").css("ime-mode", "disabled");

	});

	/******************************************
	 * 필수입력 체크
	 ******************************************/
	function saveChk() {
		if( 0 > parseFloat( J("#score").val() ) || 999.99 < parseFloat( J("#score").val() ) ){
			J.showMsgBox("최종 점수 값을 확인해 주십시오.", null, null);
			return;
		}
		if( 0 > parseFloat( J("#measScore").val() ) || 999.99 < parseFloat( J("#measScore").val() ) ){
			J.showMsgBox("계량 점수 값을 확인해 주십시오.", null, null);
			return;
		}
		if( 0 > parseFloat( J("#unmeasScore").val() ) || 999.99 < parseFloat( J("#unmeasScore").val() ) ){
			J.showMsgBox("비계량 점수 값을 확인해 주십시오.", null, null);
			return;
		}
		
		var fileChk = true;
		
		//첨부파일 확장자 체크
		J(":file", "#formList").each(function(index, item) {
			var fileFullName = J("input[name=" + item.name + "]").val();
			var src = getFileExtension(fileFullName);
			
			if (!((src.toLowerCase() == "xls") || (src.toLowerCase() == "xlsx") || (src.toLowerCase() == "ppt") || (src.toLowerCase() == "pptx")||
				  (src.toLowerCase() == "doc") || (src.toLowerCase() == "docx") || (src.toLowerCase() == "hwp") || (src.toLowerCase() == "pdf") ||
				  (src.toLowerCase() == "zip") || (src.toLowerCase() == "txt")  || (src.toLowerCase() == "gif") || (src.toLowerCase() == "jpg") ||
			      (src.toLowerCase() == "png") || (src.toLowerCase() == "png")
			    )) {
			
				fileChk = false;
    		}
		});
		
		if(fileChk == false) {
			J.showMsgBox("허용되지 않는 파일형식입니다<br/>허용되는 파일형식 : <br/>xls, xlsx, ppt, pptx, doc, docx, hwp <br/> pdf, zip, txt, gif, jpg, png, jpeg", null, null);
    		return;
		}
		
		document.charset='utf-8';
		
		J.showConfirmBox("저장하시겠습니까?", "saveDo"); //saveDo() 콜백함수 호출
	}
    
	/******************************************
	 * 저장
	 ******************************************/
	function saveDo() {
		J("#form1").attr("action", "$!{context_path}/gov/office/officeResult/officeResultProcess.vw?type=template").submit();
	}
	
	/******************************************
	 * 파일추가
	 ******************************************/
	var fileIndex = 0;
	
	function fileAttachAdd() {
		J("#fileAttach").append("<li id=\"fileRow" + fileIndex + "\"><input type=\"file\" name=\"file" + fileIndex + "\" class=\"input\" size=\"70\" /> <input type=\"button\" value=\"삭제\" onclick=\"deleteRow(" + fileIndex + ")\" class=\"input\" size=\"70\"></li>");
		fileIndex++;
	}
	
	function deleteRow(fileIndex)	{
    	J("#fileRow" + fileIndex).remove();
    }	

//]]>
</script>    
 
<form id="form1" name="form1" method="post" enctype="multipart/form-data" action="$!{context_path}/gov/office/officeResult/officeResultProcess.vw?type=template">
    <input type="hidden" 	id="mode"		    name="mode"		      value="$!{searchMap.txt2html("mode")}" />
	<input type="hidden"	id="year"           name="year"           value="$!{searchMap.txt2html("year")}" />
	<input type="hidden"	id="officeId"       name="officeId"       value="$!{searchMap.txt2html("officeId")}" />

    $!{searchMap.makeHiddenFind()}
	
    <div id="Table">
    	<div id="Table_blockContainer">
    		<div class="tableTop">
    			#parse("/WEB-INF/vl/common/template/tabs.vm")
    			<div class="clear"></div>
    		</div>
			<div class="tableTop">
                <!-- 검색 테이블 Start -->
                <div class="searchBox">
                <div><div><div>   
                    <table summary="검색조건">        
                        <caption class="none">검색조건</caption>
                        <thead><tr><th></th><th></th></tr></thead>
                        <tbody>
                            <tr>
								<td class="searchBox_tit w200">$!{YEAR} <span class="searchBar">$!{searchMap.txt2html("findYear")}년</span></td>
								<td class="searchBox_tit w350">공공기관 평가군 <span class="searchBar">$!{CS_HTML.txt2html($detail.OFFICE_EVAL_GRP_NM)}</span></td>
								<td class="searchBox_tit w250">공공기관  <span class="searchBar">$!{CS_HTML.txt2html($detail.OFFICE_NM)}</span></td>
							</tr>
                        </tbody>
                    </table>
                </div></div></div>
                </div>
                <!--// 검색 테이블 End -->
                <div class="clear"></div>
            </div>
    		<div class="blockDetail">
    			<table summary="공공기관결과 등록">
					<caption class="none">공공기관결과 등록</caption>
    				<colgroup>
    					<col width="7%" />
						<col width="7%" />
    					<col width="86%" />
    				</colgroup>
    				<tbody>
						<tr>
							<th scope="row" colspan="2"><label for="finalGrade">최종등급</label></th>
							<td><input type="text" id="finalGrade" name="finalGrade" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.FINAL_GRADE)}' maxlength="3"/></td>
						</tr>
						<tr>
							<th scope="row" colspan="2"><label for="score">점수</label></th>
							<td><input type="text" id="score" name="score" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.SCORE)}' maxlength="6"/></td>
						</tr>
						<tr>
							<th scope="row" colspan="2"><label for="rank">순위</label></th>
							<td><input type="text" id="rank" name="rank" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.RANK)}' maxlength="2"/></td>
						</tr>
						<tr>
							<th scope="row" rowspan="2" class="textCenter">계량</th>
							<th scope="row"><label for="measScore">점수</label></th>
							<td><input type="text" id="measScore" name="measScore" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.MEAS_SCORE)}' maxlength="6"/></td>
						</tr>
						<tr>
							<th scope="row"><label for="measRank">순위</label></th>
							<td><input type="text" id="measRank" name="measRank" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.MEAS_RANK)}' maxlength="2"/></td>
						</tr>
						<tr>
							<th scope="row" rowspan="2" class="textCenter">비계량</th>
							<th scope="row"><label for="unmeasScore">점수</label></th>
							<td><input type="text" id="unmeasScore" name="unmeasScore" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.UNMEAS_SCORE)}' maxlength="6"/></td>
						</tr>
						<tr>
							<th scope="row"><label for="unmeasRank">순위</label></th>
							<td><input type="text" id="unmeasRank" name="unmeasRank" class="inputbox w200" value='$!{CS_HTML.txt2html($detail.UNMEAS_RANK)}' maxlength="2"/></td>
						</tr>

						<tr>
    						<th scope="row" colspan="2">첨부파일</th>
    						<td colspan="3">
								#foreach($fileList in $fileList)
									$!{CS_HTML.makeFileIconFileName($fileList.get("ATTACH_FILE_SUFFIX"), $fileList.get("ATTACH_FILE_NM"), $fileList.get("ATTACH_FILE_PATH"))} 
									&nbsp;&nbsp;<input type="checkbox" name="delAttachFiles" id="delAttachFile$!{fileList.get("SEQ")}" value="$!{fileList.get("SEQ")}" />&nbsp;<label for="delAttachFile$!{fileList.get("SEQ")}">첨부파일 삭제</label> <br/>
								#end
								<a href="javascript:fileAttachAdd();"><img src="$!{img_path}/common/btn_file.gif" class="valign_middle" alt="파일추가" /></a>&nbsp;(첨부파일 용량제한은 100M입니다)<br/>
								<div>
									<ul id="fileAttach">
										<li></li>
									</ul>
								</div>
							</td>
    					</tr>
    				</tbody>
    			</table>
    		</div>
    		<div class="blockButton"> 
    			<ul>
    				<li><input type="button" id="btnBack"   value="돌아가기" /></li>
    				<li><input type="submit" id="btnInsert" value="저장" /></li>
    			</ul>
    		</div>	
    	</div>
    	<div class="clear"></div>
    </div>
</form>
