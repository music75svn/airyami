<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="bsc.popup.ovrsBusSptEntSupportPerf">

	<!--
	==================================================================
	  # 설명	: typeAlias 선언
	  #	기능	: SETTING
	  #	TABLE	:
	==================================================================
	-->
	<typeAlias alias="hashMap" 			type="java.util.HashMap" />


	<!--
	==================================================================
	  # 설명	: 시스템항목관리 목록 보기
	  #	기능	: SELECT
	  #	TABLE	: BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getList" parameterClass="hashMap" resultClass="hashMap">

		<!-- 해외사업지원처 공장등록심사 가중치합(S051D001) -->
		<isEqual property="findItemCd" compareValue="S051D001">
		SELECT A.YEAR
		      ,A.MON
		      ,A.DEPT_CD
		      ,B.DEPT_NM
		      ,A.RPT_TC_NM
		      ,A.ROLE_TC_NM
		      ,A.INSP_KIND_TC_NM
		      ,A.LAW_TC_NM
		      ,A.STIC_LAG_TC_NM
		      ,A.STIC_MID_TC_NM
		      ,A.STIC_SML_TC_NM
		      ,A.INSP_QTY
		      ,CASE
		          WHEN A.ROLE_TC = '04' AND A.INSP_KIND_TC = '06' THEN A.INSP_QTY * 13577.2
		          WHEN A.ROLE_TC = '12' AND A.INSP_KIND_TC = '01' THEN A.INSP_QTY * 2866.5
		          WHEN A.ROLE_TC = '13' AND A.INSP_KIND_TC = '03' THEN A.INSP_QTY * 313.6
		          ELSE 0
		       END
		          INSP_QTY_RST
		  FROM TABLE (BSC_SYSTEM_KGS.F_V_GHF0221D_LIST(#findYear#, #findMon#)) A
		  INNER JOIN BSC_DEPTINFO B
		  ON A.DEPT_CD = B.DEPT_ID
		 AND  A.ROLE_TC = '04' AND A.INSP_KIND_TC = '06'
		    AND DEPT_CD = #findDeptId#
		</isEqual>

		<!-- 해외사업지원처 방폭수탁 가중치합(S051D002) -->
		<isEqual property="findItemCd" compareValue="S051D002">
			SELECT A.YEAR
			      ,A.MON
			      ,A.DEPT_CD
			      ,B.DEPT_NM
			      ,A.RPT_TC_NM
			      ,A.ROLE_TC_NM
			      ,A.INSP_KIND_TC_NM
			      ,A.LAW_TC_NM
			      ,A.STIC_LAG_TC_NM
			      ,A.STIC_MID_TC_NM
			      ,A.STIC_SML_TC_NM
			      ,A.INSP_QTY
			      ,CASE
			          WHEN A.ROLE_TC = '04' AND A.INSP_KIND_TC = '06' THEN A.INSP_QTY * 13577.2
			          WHEN A.ROLE_TC = '12' AND A.INSP_KIND_TC = '01' THEN A.INSP_QTY * 2866.5
			          WHEN A.ROLE_TC = '13' AND A.INSP_KIND_TC = '03' THEN A.INSP_QTY * 313.6
			          ELSE 0
			       END
			          INSP_QTY_RST
			  FROM TABLE (BSC_SYSTEM_KGS.F_V_GHF0221D_LIST(#findYear#, #findMon#)) A
			  INNER JOIN BSC_DEPTINFO B
			  ON A.DEPT_CD = B.DEPT_ID
			 WHERE  A.ROLE_TC = '12' AND A.INSP_KIND_TC = '01'
			       AND A.DEPT_CD = #findDeptId#
                           AND A.STIC_LAG_TC = '86'
                           AND A.STIC_MID_TC = '00'
                           AND A.STIC_SML_TC = '00'
		</isEqual>

		<!-- 해외사업지원처 방폭안정(인증) 가중치합(S051D003) -->
		<isEqual property="findItemCd" compareValue="S051D003">
			SELECT A.YEAR
			      ,A.MON
			      ,A.DEPT_CD
			      ,B.DEPT_NM
			      ,A.RPT_TC_NM
			      ,A.ROLE_TC_NM
			      ,A.INSP_KIND_TC_NM
			      ,A.LAW_TC_NM
			      ,A.STIC_LAG_TC_NM
			      ,A.STIC_MID_TC_NM
			      ,A.STIC_SML_TC_NM
			      ,A.INSP_QTY
			      ,CASE
			          WHEN A.ROLE_TC = '04' AND A.INSP_KIND_TC = '06' THEN A.INSP_QTY * 13577.2
			          WHEN A.ROLE_TC = '12' AND A.INSP_KIND_TC = '01' THEN A.INSP_QTY * 2866.5
			          WHEN A.ROLE_TC = '13' AND A.INSP_KIND_TC = '03' THEN A.INSP_QTY * 313.6
			          ELSE 0
			       END
			          INSP_QTY_RST
			  FROM TABLE (BSC_SYSTEM_KGS.F_V_GHF0221D_LIST(#findYear#, #findMon#)) A
			  INNER JOIN BSC_DEPTINFO B
			  ON A.DEPT_CD = B.DEPT_ID
			 WHERE A.ROLE_TC = '13' AND A.INSP_KIND_TC = '03'
			       AND A.DEPT_CD = #findDeptId#
			       AND A.STIC_MID_TC IN ('30', '05', '25')
		</isEqual>
	</select>


	<!--
	==================================================================
	  # 설명	: 부서조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_DEPTINFO
	==================================================================
	-->
	<select id="getDeptList" parameterClass="hashMap" resultClass="hashMap">
		SELECT A.DEPT_ID, B.DEPT_NM
		  FROM BSC_SC_DEPT_MAPPING A
	LEFT OUTER JOIN BSC_DEPTINFO B
			ON A.DEPT_ID = B.DEPT_ID
		 WHERE (YEAR, SC_DEPT_ID) IN
		          (SELECT YEAR, SC_DEPT_ID
		             FROM BSC_METRIC
		            WHERE (METRIC_GRP_ID, YEAR) IN
		                     (SELECT METRIC_GRP_ID, YEAR
		                        FROM BSC_METRIC
		                       WHERE YEAR = #findYear# AND METRIC_ID = #findMetricId#)
		            AND DELETE_DT IS NULL)

	</select>


	<!--
	==================================================================
	  # 설명	: 연계개수 팝업 데이터 조회
	  #	기능	: SELECT
	  #	TABLE	: BSC_CAL_TYPE_COL, BSC_SYSTEM_ITEM
	==================================================================
	-->
	<select id="getPopItemCntList" parameterClass="hashMap" resultClass="hashMap">
		SELECT   A.YEAR
		       , C.SC_DEPT_ID
		       , D.SC_DEPT_NM
		       , A.METRIC_ID
		       , C.METRIC_NM
		       , A.CAL_TYPE_COL
		       , A.CAL_TYPE_COL_NM
		       , B.ITEM_CD
		       , B.ITEM_NM
		    FROM BSC_CAL_TYPE_COL A INNER JOIN BSC_SYSTEM_ITEM B ON A.YEAR = B.YEAR
		                                                       AND A.ITEM_CD = B.ITEM_CD
		                                                       AND B.DELETE_DT IS NULL
		         INNER JOIN BSC_METRIC C ON A.YEAR = C.YEAR
		                               AND A.METRIC_ID = C.METRIC_ID
		                               AND C.DELETE_DT IS NULL
		         INNER JOIN BSC_SC_DEPT D ON C.YEAR = D.YEAR
		                                AND C.SC_DEPT_ID = D.SC_DEPT_ID
		                                AND D.DELETE_DT IS NULL
		   WHERE A.YEAR = #findYear#
		     AND A.ITEM_CD = #findItemCd#
		     AND A.DELETE_DT IS NULL
		     AND A.ITEM_CD IS NOT NULL
		ORDER BY A.METRIC_ID, A.CAL_TYPE_COL
	</select>

	<!--
	==================================================================
	  # 설명	: 시스템 연계항목 정보를 가져옴
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getItem" remapResults="true" parameterClass="hashMap" resultClass="hashMap">
          SELECT A.ITEM_CD, B.CAL_TYPE_COL_NM
            FROM BSC_SYSTEM_ITEM A
               , BSC_CAL_TYPE_COL B
           WHERE A.YEAR = #findYear#
             AND A.YEAR = B.YEAR
             AND A.ITEM_CD = B.ITEM_CD
             AND B.METRIC_ID = #findMetricId#
             AND B.INSERT_GUBUN = '01'
           ORDER BY B.CAL_TYPE_COL_NM
	</select>

	<!--
	==================================================================
	  # 설명	: 시스템 연계 부서 목록 보기
	  #	기능	: SELECT
	  #	TABLE	:
	==================================================================
	-->
	<select id="getScDeptMapping" parameterClass="hashMap" resultClass="java.lang.String">
		SELECT DEPT_ID
		  FROM ( SELECT DEPT_ID
				   FROM BSC_SC_DEPT_MAPPING
				  WHERE YEAR = #findYear#
				    AND SC_DEPT_ID = #findScDeptCd# ORDER BY DEPT_ID )
		 WHERE ROWNUM = 1
	</select>

</sqlMap>
