#*************************************************************************
* CLASS 명      : evalConItemModify
* 작 업 자      : 김효은 
* 작 업 일      : 2014년 3월 19일 
* 기    능      : 직원개인기여도평가 평가항목    
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자    작     업     일        변 경 내 용                비고
* ----  --------  -----------------  -------------------------  --------
*  1    김효은      2014년 3월 19일             최 초 작 업 
**************************************************************************# 

<script type="text/javascript" language="javascript" >
//<![CDATA[

    var J = jQuery;

    J(document).ready(function(){
    
        /******************************************
         * 취소버튼 (돌아가기)
         ******************************************/
        J("#btnBack").click(function() {
            J("#form1").attr("action", "$!{context_path}/prs/evalCon/evalConItem/evalConItemList.vw?type=template").submit();
        });
        
        /******************************************
         * 저장버튼 이벤트
         ******************************************/
        J("#btnInsert").click(function(e) {
            e.preventDefault();
            saveChk();
        });

        /******************************************
         * 저장버튼 이중 클릭 방지
         ******************************************/
        J("form").submit(function(){
            J("#btnInsert").unbind();
        });
		
		/******************************************
		 * 비고 byte 체크
		 ******************************************/
		J("#content").checkbyte({
			indicator:J("#indicator"),
			limit:2000
		});
		J("#content").trigger("keyup");
		
		/******************************************
		 * 가중치/정렬순서 숫자만 입력
		 ******************************************/
		J("#sortOrder").numeric();
		J("#sortOrder").css("ime-mode", "disabled");
		J("#weight").numeric();
		J("#weight").css("ime-mode", "disabled");

    });

    /******************************************
     * 필수입력 체크
     ******************************************/
    function saveChk() {
		/*var totalbefore = 0;
		var total = 0;
		var weightTotal = J("#sumAdd").val();
		var beforeWeight = "$!{CS_HTML.txt2html($detail.WEIGHT)}";*/
	
		if(!J.checkLength("evalItemNm", "평가항목명", 1, 100)) {
			return false;
		}
		if(J("#weight").val() == ""){
			J.showMsgBox("가중치를 입력해주십시오.", null, "weight");
			return false;
		}
		
		/*if(J("#mode").val() == "MOD"){//수정일때
			totalbefore = parseFloat(weightTotal) - parseFloat(beforeWeight);
	 	    total  = parseFloat(totalbefore) + parseFloat(J("#weight").val());
		}else{
			total  = parseFloat(weightTotal) + parseFloat(J("#weight").val());
		}
		
		total= Math.round(total*100)/100;
		
		if(total > "100"){
		 J.showMsgBox("가중치 합계 100이 넘어갑니다.<br/>다시 조정해 주십시오.", null, "weight");
		 return false;
		}*/
    
        J.showConfirmBox("저장하시겠습니까?", "saveDo"); //saveDo() 콜백함수 호출
    }
    
    /******************************************
     * 저장
     ******************************************/
    function saveDo() {
        J("#form1").attr("action", "$!{context_path}/prs/evalCon/evalConItem/evalConItemProcess.vw?type=template").submit();
    }

//]]>
</script>    
 
<form id="form1" name="form1" method="post" action="$!{context_path}/prs/evalCon/evalConItem/evalConItemProcess.vw?type=template">
    <input type="hidden"     id="mode"            name="mode"              value="$!{searchMap.txt2html("mode")}" />
	<input type="hidden" 	 id="sumAdd" 		  name="sumAdd" 	  	   value="$!{searchMap.txt2html("sumAdd")}" />
    $!{searchMap.makeHiddenFind()}
    
    <div id="Table">
        <div id="Table_blockContainer">
            <div class="tableTop">
                #parse("/WEB-INF/vl/common/template/tabs.vm")
                <div class="clear"></div>
            </div>
            <div class="tableTop">
                <!-- 검색 테이블 Start -->
                <div class="searchBox">
                <div><div><div>   
                    <table summary="검색조건">        
                        <caption class="none">검색조건</caption>
                        <thead><tr><th></th><th></th></tr></thead>
                        <tbody>
                            <tr>
                                <td class="searchBox_tit w250">$!{YEAR} <span class="searchBar">$!{searchMap.txt2html("findYear")}년</span></td>
                            </tr>
                        </tbody>
                    </table>
                </div></div></div>
                </div>
                <!--// 검색 테이블 End -->
                <div class="clear"></div>
            </div>
            <div class="blockDetail">
                <table summary="직원개인기여도평가 평가항목 등록">
                    <caption class="none">직원개인기여도평가 평가항목 등록</caption>
                    <colgroup>
                        <col width="15%" />
                        <col width="85%" />
                    </colgroup>
                    <tbody>
                        <tr>
                            <th scope="row"><label for="evalItemId">평가항목코드</label></th>
                            <td>
								#if($!{searchMap.txt2html("mode")} == "MOD")
									$!{CS_HTML.txt2html($detail.EVAL_ITEM_ID)}
									<input type="hidden" id="evalItemId" name="evalItemId" class="inputbox w100" value="$!{CS_HTML.txt2html($detail.EVAL_ITEM_ID)}" maxlength="3" readonly="readonly" />
								#else
									<input type="hidden" id="evalItemId" name="evalItemId" class="inputbox w100" maxlength="3" />
									※ 저장시 자동으로 부여됩니다.
								#end
                            </td>
                        </tr>
						<tr>
    						<th scope="row"><span class="txt_red">(*)</span><label for="evalItemNm">평가항목 명</label></th>
    						<td><input type="text" id="evalItemNm" name="evalItemNm" class="inputbox w385" value='$!{CS_HTML.txt2html($detail.EVAL_ITEM_NM)}' maxlength="250"/></td>
    					</tr>
						<tr>
    						<th scope="row"><span class="txt_red">(*)</span><label for="evalGbn">평가구분</label></th>
    						<td>
    							<select id="evalGbn" name="evalGbn" class="select w150">
    								$!CS_CODE.getCodeOption("226", $detail.EVAL_GBN, $!{searchMap.txt2html("findYear")})
    							</select>
    						</td>
    					</tr>
						<tr>
    						<th scope="row"><span class="txt_red">(*)</span><label for="weight">가중치</label></th>
    						<td>
								<input type="text" id="weight" name="weight" class="inputbox w140" value='$!{CS_HTML.txt2html($detail.WEIGHT)}' maxlength="3"/><span class="strong"> %</span>
							</td>
    					</tr>
						<tr>
    						<th scope="row">&nbsp;<label for="sortOrder">정렬순서</label></th>
							<td><input type="text" id="sortOrder" name="sortOrder" class="inputbox w140" value='$!{CS_HTML.txt2html($detail.SORT_ORDER)}' maxlength="5"/></td>
						</tr>
						<tr>
    						<th scope="row"><label for="content">설명</label></th>
    						<td>
    							<textarea id="content" name="content" class="tabletext" title="설명" cols="" rows="10">$!{CS_HTML.txt2html($detail.CONTENT)}</textarea>
								<span id="indicator">0</span>/2000byte
    						</td>
    					</tr>
                    </tbody>
                </table>
            </div>
            <div class="blockButton"> 
                <ul>
                    <li><input type="button" id="btnBack"   value="돌아가기" /></li>
                    <li><input type="submit" id="btnInsert" value="저장" /></li>
                </ul>
            </div>    
        </div>
        <div class="clear"></div>
    </div>
</form>
