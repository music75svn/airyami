#*************************************************************************
* CLASS 명      : evalResultList_xml
* 작 업 자      : 하윤식
* 작 업 일      : 2012년 10월 25일 
* 기    능      : 평가결과 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      하윤식     2012년 10월 25일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"                 : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"EVAL_USER_GRP_ID"     : "$!{CS_HTML.txt2html($list.EVAL_USER_GRP_ID)}", 
		"METRIC_ID"            : "$!{CS_HTML.txt2html($list.METRIC_ID)}", 
		"METRIC_NM"            : "$!{CS_HTML.txt2html($list.METRIC_NM)}", 
		"WEIGHT"               : "$!{CS_HTML.txt2html($list.WEIGHT)}", 
		"SC_DEPT_NM"           : "$!{CS_HTML.txt2html($list.SC_DEPT_NM)}", 
		"USER_NM"              : "$!{CS_HTML.txt2html($list.USER_NM)}", 
		
		#foreach($itemList in $itemList)
			"ITEM_$!{CS_HTML.txt2html($itemList.ITEM_ID)}" : "$!{CS_HTML.txt2html($list.get($itemList.ITEM_COL_NM))}" ,
		#end
		
		#foreach($itemGrpList in $itemGrpList)
			"ITEM_GRP_SCORE_$!{CS_HTML.txt2html($itemGrpList.ITEM_GRP_ID)}" : "$!{CS_HTML.txt2html($list.get($itemGrpList.ITEM_GRP_SCORE_COL_NM))}",
		#end
		
		#foreach($itemGrpList in $itemGrpList)
			"ITEM_GRP_GRADE_$!{CS_HTML.txt2html($itemGrpList.ITEM_GRP_ID)}" : "$!{CS_HTML.txt2html($list.get($itemGrpList.ITEM_GRP_GRADE_COL_NM))}",
		#end
		 
		"ADJUST_RATE"          : "$!{CS_HTML.txt2html($list.ADJUST_RATE)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
