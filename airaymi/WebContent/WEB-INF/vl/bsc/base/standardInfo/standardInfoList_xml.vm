#*************************************************************************
* CLASS 명      : standardInfoList_xml
* 작 업 자      : 하윤식
* 작 업 일      : 2012년 11월 28일 
* 기    능      : 평가기준 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      하윤식     2012년 11월 28일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"                 : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"COMM_METRIC_ADJUST_PER" : "$!{CS_HTML.txt2html($list.COMM_METRIC_ADJUST_PER)}", 
		"UNIQ_METRIC_ADJUST_PER" : "$!{CS_HTML.txt2html($list.UNIQ_METRIC_ADJUST_PER)}", 
		"CREATE_DT"            : "$!{CS_HTML.txt2html($list.CREATE_DT)}", 
		"MODIFY_DT"            : "$!{CS_HTML.txt2html($list.MODIFY_DT)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
