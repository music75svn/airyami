#*************************************************************************
* CLASS 명      : matrixEvalResultList_xml.vm
* 작 업 자      : 정철수
* 작 업 일      : 2012년 10월 23일 
* 기    능      : 평가내역조회 팝업의  json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      정철수     2012년 10월 23일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		 
		"STRATEGY_NM"        : "$!{CS_HTML.txt2html($list.STRATEGY_NM)}"
		, "POOL_GUBUN_NM"        : "$!{CS_HTML.txt2html($list.POOL_GUBUN_NM)}"
		, "METRIC_GRP_NM"        : "$!{CS_HTML.txt2html($list.METRIC_GRP_NM)}"
		#foreach($subList in $itemIdList)
			, "$!{CS_HTML.txt2html($subList.ITEM_ID)}" : "$!{CS_HTML.txt2html($list.get($subList.ITEM_ID))}"
		#end
		, "METRIC_GRP_ID"        : "$!{CS_HTML.txt2html($list.METRIC_GRP_ID)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}

