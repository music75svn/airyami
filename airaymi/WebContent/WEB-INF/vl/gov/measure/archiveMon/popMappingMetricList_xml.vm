#*************************************************************************
* CLASS 명      : popMappingMetricList_xml.vm
* 작 업 자      : 하윤식
* 작 업 일      : 2012년 11월 22일 
* 기    능      : 경평지표 매핑 리스트
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1     하윤식      2012.11.22            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"         	 : "$!{CS_HTML.txt2html($list.YEAR)}",
        "GOV_METRIC_ID"  : "$!{CS_HTML.txt2html($list.GOV_METRIC_ID)}",
        "METRIC_ID"      : "$!{CS_HTML.txt2html($list.METRIC_ID)}",
        "SC_DEPT_NM"     : "$!{CS_HTML.txt2html($list.SC_DEPT_NM)}",
        "METRIC_NM"      : "$!{CS_HTML.txt2html($list.METRIC_NM)}",
        "TARGET"         : "$!{CS_HTML.txt2html($list.TARGET)}",
        "ACTUAL"         : "$!{CS_HTML.txt2html($list.ACTUAL)}",
        "SCORE"          : "$!{CS_HTML.txt2html($list.SCORE)}",
        "STATUS"         : "$!{CS_HTML.txt2html($list.STATUS)}"
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
