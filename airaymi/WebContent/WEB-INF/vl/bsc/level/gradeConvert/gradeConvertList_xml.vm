#*************************************************************************
* CLASS 명      : gradeConvertList_xml
* 작 업 자      : 하윤식
* 작 업 일      : 2012년 10월 23일 
* 기    능      : 난이도등급점수 json 데이터 조회
* ---------------------------- 변 경 이 력 --------------------------------
* 번호    작 업 자     작   업   일       변 경 내 용           비  고
* ----  --------  -----------------  -------------------------  -----------
*  1      하윤식     2012년 10월 23일            최 초 작 업
**************************************************************************# 
#set($listSize = $!{CS_HTML.txt2html($list.size())})
#set($idx = 0)

{
	"page": "1",
	"total": 1,
	"records": "$listSize",
	"rows":[

#foreach($list in $list)
	{
		"YEAR"                 : "$!{CS_HTML.txt2html($list.YEAR)}", 
		"ITEM_GRP_ID"          : "$!{CS_HTML.txt2html($list.ITEM_GRP_ID)}", 
		"ITEM_GRP_NM"          : "$!{CS_HTML.txt2html($list.ITEM_GRP_NM)}"
		
		#foreach($subList in $gradeList)
			, "MIN_$!{CS_HTML.txt2html($subList.GRADE)}" : "$!{CS_HTML.txt2html($list.get($subList.MIN_COL))}"
		#end
		
		#foreach($subList in $gradeList)
			, "MAX_$!{CS_HTML.txt2html($subList.GRADE)}" : "$!{CS_HTML.txt2html($list.get($subList.MAX_COL))}"
		#end
		
		#foreach($subList in $gradeList)
			, "VAL_$!{CS_HTML.txt2html($subList.GRADE)}" : "$!{CS_HTML.txt2html($list.get($subList.MIN_COL))} ~ $!{CS_HTML.txt2html($list.get($subList.MAX_COL))}"
		#end
	}
	#set($idx = $idx + 1)
	#if($idx != $listSize)
	 ,
	#end
#end

	]
}
